version: "3.7"
services:

  app:
    image: nutriplan-pro
    build:
      context: .
      args:
        NEXT_PUBLIC_SUPABASE_URL: ${NEXT_PUBLIC_SUPABASE_URL}
        NEXT_PUBLIC_SUPABASE_ANON_KEY: ${NEXT_PUBLIC_SUPABASE_ANON_KEY}
    networks:
      - typebotnet
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      labels:
        - "traefik.enable=true"
        - "traefik.docker.network=typebotnet"
        - "traefik.http.routers.nutriplanpro.rule=Host(`nutriplanpro.automatech.tech`)"
        - "traefik.http.routers.nutriplanpro.entrypoints=websecure"
        - "traefik.http.routers.nutriplanpro.tls.certresolver=letsencryptresolver"
        - "traefik.http.routers.nutriplanpro.service=nutriplanpro"
        - "traefik.http.services.nutriplanpro.loadbalancer.server.port=80"

networks:
  typebotnet:
    external: true
    name: typebotnet
